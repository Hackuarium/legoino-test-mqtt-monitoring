'use strict';

const testMqttMonitoring = require('..');

describe('test-mqtt-monitoring test', () => {
    it('Something to test', () => {
        expect(testMqttMonitoring()).toBe(true);
    });
});
